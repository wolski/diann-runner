# The repo-root .gitignore ignores *.tsv — but input_dataset.tsv IS the committed
# SUSHI test input, so un-ignore it.
!input_dataset.tsv

# Generated by `./run.sh run` (outputs land in this dir; SUSHI delivers via g-req).
out-DIANN_*/
temp-DIANN_*/
.snakemake/
logs/
qc_result/
*.zip
*.log
step_*.sh
outputs.yml
diann_runner_params.toml
dataset.csv
